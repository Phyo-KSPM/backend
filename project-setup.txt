================================================================================
  CEIR Backend — Project Setup & Start Guide
  ဖိုင်: project-setup.txt
================================================================================

ဤဖိုင်က project ကို မ run ခင် / စတင်ပုံကို အသေးစိတ် ရှင်းပြသည်။
အမြန်အတိုချုပ်: project-start.txt
GitHub: https://github.com/Phyo-KSPM/backend.git


################################################################################
  ပတ်ဝန်းကျင် မူဝါဒ (အရေးကြီး)
################################################################################

  Windows local (dev)
    → PostgreSQL + Redis = Docker
    → npm run docker:infra   (သို့ npm run local:infra)
    → App = PM2 သို့ npm run dev:all

  Linux production (/opt/ceir/backend)
    → PostgreSQL + Redis = host ပေါ် တိုက်ရိုက် တပ် (apt/system packages)
    → Docker / docker:infra = မသုံးရ
    → App = PM2 သာ

  package.json ထဲ docker:* နှင့် local:infra* scripts များသည်
  Windows local အတွက်သာ ဖြစ်သည်။


################################################################################
  အပိုင်း A — Windows local (Docker သုံး)
################################################################################


--------------------------------------------------------------------------------
A1. လိုအပ်သော software
--------------------------------------------------------------------------------

  [ ] Node.js 20+
  [ ] Docker Desktop (Running)
  [ ] Git (optional)
  [ ] PowerShell / CMD


--------------------------------------------------------------------------------
A2. Folder + install + .env
--------------------------------------------------------------------------------

  cd C:\Users\rnd\Desktop\project-setup\backend
  npm install
  copy .env.example .env

  .env (local Docker default):

    DATABASE_URL=postgresql://postgres:postgres@localhost:5432/app_db
    REDIS_URL=redis://localhost:6379
    JWT_SECRET=change-me-in-production


--------------------------------------------------------------------------------
A3. Infra စတင် (Docker — Windows only)
--------------------------------------------------------------------------------

  Docker Desktop Ready ဖြစ်မှ:

    npm run docker:infra

  (alias) npm run local:infra

  စစ်ရန်:

    docker ps --filter "name=ceir-"

  (healthy) ဖြစ်သည်အထိ စောင့်

  ရပ်ရန်:

    npm run docker:infra:down


--------------------------------------------------------------------------------
A4. Migrate + seed
--------------------------------------------------------------------------------

    npm run db:setup


--------------------------------------------------------------------------------
A5. App စတင် (PM2)
--------------------------------------------------------------------------------

    npm run pm2:start
    npm run pm2:status

  Foreground debug:

    npm run pm2:delete
    npm run dev:all


--------------------------------------------------------------------------------
A6. Smoke test
--------------------------------------------------------------------------------

    http://localhost:3000/health
    http://localhost:3000/openapi/v1/nrc/townships

  Login: maung@dealer.com / secret123
  IMEI:  359876543210108


--------------------------------------------------------------------------------
A7. Windows နေ့စဉ်
--------------------------------------------------------------------------------

    1. Docker Desktop ဖွင့်
    2. npm run docker:infra
    3. npm run pm2:start
    4. npm run pm2:status

  ရပ်:

    npm run pm2:delete
    npm run docker:infra:down


--------------------------------------------------------------------------------
A8. Windows — Docker scripts (package.json)
--------------------------------------------------------------------------------

  npm run docker:infra / local:infra       Postgres + Redis containers
  npm run docker:infra:down / local:infra:down
  npm run docker:db / docker:redis
  npm run docker:up / docker:down          full stack (optional)


################################################################################
  အပိုင်း B — Linux production (/opt/ceir/backend) — Docker မသုံး
################################################################################


--------------------------------------------------------------------------------
B1. လိုအပ်သော software (host)
--------------------------------------------------------------------------------

  [ ] Node.js 20+
  [ ] PostgreSQL (apt)
  [ ] Redis (apt)
  [ ] Git
  [ ] ★ Docker မလို / မသုံး


--------------------------------------------------------------------------------
B2. Host PostgreSQL + Redis တပ်ခြင်း (ဥပမာ Ubuntu)
--------------------------------------------------------------------------------

  sudo apt update
  sudo apt install -y postgresql postgresql-contrib redis-server

  sudo systemctl enable --now postgresql
  sudo systemctl enable --now redis-server

  # DB + user ဥပမာ
  sudo -u postgres psql -c "CREATE USER ceir WITH PASSWORD 'your_strong_password';"
  sudo -u postgres psql -c "CREATE DATABASE app_db OWNER ceir;"
  sudo -u postgres psql -c "GRANT ALL PRIVILEGES ON DATABASE app_db TO ceir;"

  Redis စစ်ရန်:

    redis-cli ping
    # PONG


--------------------------------------------------------------------------------
B3. Project clone + .env
--------------------------------------------------------------------------------

  sudo mkdir -p /opt/ceir
  sudo chown -R $USER:$USER /opt/ceir
  cd /opt/ceir
  git clone https://github.com/Phyo-KSPM/backend.git backend
  cd /opt/ceir/backend

  cp .env.example .env
  nano .env

  .env ဥပမာ (host services):

    DATABASE_URL=postgresql://ceir:your_strong_password@localhost:5432/app_db
    DB_HOST=localhost
    DB_PORT=5432
    DB_USER=ceir
    DB_PASS=your_strong_password
    DB_DATABASE=app_db
    REDIS_URL=redis://localhost:6379
    JWT_SECRET=change-me-in-production

  ★ npm run docker:infra   မလုပ်ရ
  ★ npm run docker:*       မလုပ်ရ


--------------------------------------------------------------------------------
B4. Install + migrate + PM2
--------------------------------------------------------------------------------

  npm install
  npm run db:setup
  npm run pm2:start
  npm run pm2:status

  Reboot ပြီး auto-start (optional):

    npx pm2 save
    npx pm2 startup


--------------------------------------------------------------------------------
B5. Linux နေ့စဉ်
--------------------------------------------------------------------------------

  # Postgres/Redis က systemctl နဲ့ အလိုအလျောက် စနေမယ် (enable ထားရင်)

  cd /opt/ceir/backend
  npm run pm2:start
  npm run pm2:status

  Update:

    cd /opt/ceir/backend
    git pull
    # REQUIRED: set a strong JWT_SECRET in .env (>=32 chars), unique per environment
    # node -e "console.log(require('crypto').randomBytes(48).toString('base64url'))"
    # Production: SWAGGER_ENABLED=false (or omit), set CORS_ORIGINS if browsers call the API
    npm install
    npm run db:setup
    npm run pm2:delete
    npm run pm2:start


--------------------------------------------------------------------------------
B6. ecosystem path
--------------------------------------------------------------------------------

  Production root: /opt/ceir/backend

  resolve အစဉ်:
    1) CEIR_HOME
    2) /opt/ceir/backend/package.json ရှိရင် → /opt/ceir/backend
    3) __dirname

  Logs: /opt/ceir/backend/logs/


################################################################################
  အပိုင်း C — PM2 commands (Windows + Linux တူ)
################################################################################

  npm run pm2:start
  npm run pm2:status
  npm run pm2:logs
  npm run pm2:restart
  npm run pm2:reload
  npm run pm2:stop
  npm run pm2:delete
  npm run pm2:flush

  Config: ecosystem.config.cjs


################################################################################
  အပိုင်း D — Services / Ports
################################################################################

  api-gateway          3000
  bff                  3002
  auth-service         3010
  users-service        3011
  devices-service      3012
  tax-service          3013
  payments-service     3014
  claims-service       3015
  activities-service   3016
  nrc-service          3017
  postgres             5432
  redis                6379


################################################################################
  အပိုင်း E — API အကျဉ်း
################################################################################

  Base: http://localhost:3000/openapi/v1

  POST /login
  POST /auth/refresh
  GET  /device/binding
  POST /device/bind
  GET  /profile
  POST /dealer/verify
  POST /imei/check
  POST /imei/bulk-check
  POST /tax/applications
  GET  /tax/applications/{id}
  POST /payments/batches
  POST /payments/batches/{id}/pay
  GET  /payments
  GET  /payments/{id}
  POST /claims
  GET  /claims
  GET  /activities
  GET  /nrc/townships


################################################################################
  အပိုင်း F — Demo credentials
################################################################################

  Email:       maung@dealer.com
  Password:    secret123
  Sample IMEI: 359876543210108


################################################################################
  အပိုင်း G — Troubleshooting
################################################################################

  Windows: Docker daemon is not running
    → Docker Desktop ဖွင့်

  Linux: connection refused :5432 / :6379
    → sudo systemctl status postgresql
    → sudo systemctl status redis-server
    → .env DATABASE_URL / REDIS_URL စစ်

  Linux မှာ docker:infra error
    → Production မှာ Docker မသုံးရ — host Postgres/Redis သုံးပါ

  Port already in use
    → pm2:status / (Windows) docker ps

  relation "users" does not exist
    → npm run db:setup

  Login 401
    → seed + demo password စစ်

  ACCOUNT_BOUND_TO_OTHER_DEVICE
    → တူညီသော deviceFingerprint သုံး


################################################################################
  အပိုင်း H — Folder
################################################################################

  database\                 migrations/seeds + compose (Windows local)
  redis\                    compose (Windows local)
  docker-compose*.yml       Windows local only
  ecosystem.config.cjs      PM2
  services\
  packages\shared\
  .env / .env.example
  project-setup.txt / project-start.txt / README.md


################################################################################
  အပိုင်း I — Git clone
################################################################################

  Windows local:

    git clone https://github.com/Phyo-KSPM/backend.git
    cd backend
    npm install
    copy .env.example .env
    npm run docker:infra
    npm run db:setup
    npm run pm2:start

  Linux production:

    git clone https://github.com/Phyo-KSPM/backend.git /opt/ceir/backend
    cd /opt/ceir/backend
    cp .env.example .env
    # edit .env for host Postgres/Redis
    npm install
    npm run db:setup
    npm run pm2:start
    # docker:infra မလုပ်


################################################################################
  အမြန် copy-paste
################################################################################

  --- Windows local ---

    cd C:\Users\rnd\Desktop\project-setup\backend
    npm install
    npm run docker:infra
    npm run db:setup
    npm run pm2:start

  --- Linux production ---

    cd /opt/ceir/backend
    npm install
    npm run db:setup
    npm run pm2:start

================================================================================
  အတိုချုပ်

    Windows  → Docker infra → db:setup → pm2:start
    Linux    → host Postgres/Redis → db:setup → pm2:start  (no Docker)
================================================================================
